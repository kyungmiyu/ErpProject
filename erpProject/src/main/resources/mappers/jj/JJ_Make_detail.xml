<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oracle.erpProject.Work_order_detailMapper">

<!-- ============================ JJ's mappers start ============================ -->
	<!-- 생산게시판 글의 총 갯수 파악하는 쿼리 -->
	<select id="jjcntMake" parameterType="int" resultSets="int">
		SELECT Count(*)
		FROM Make_detail
	</select>

	<!-- 생산게시판 전체 게시글을 불러오는 쿼리 -->
	<select id="jjMakeList" parameterType="Make_detail" resultType="Make_detail">
		<![CDATA[
		SELECT *
		FROM (
			SELECT ROWNUM rn, w.*, p.p_name
			FROM (
				SELECT *
				FROM Make_detail
				ORDER BY wod_num desc) w
			JOIN Product p ON w.p_itemcode = p.p_itemcode
		)
		WHERE rn >= #{start}
		AND rn <= #{end}
		]]>
	</select>
	


<!-- ============================ JJ's mappers 끝 ============================ -->

</mapper>